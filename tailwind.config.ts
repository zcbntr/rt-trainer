import type { Config } from 'tailwindcss';

export default {
	// Tailwind v4 prefers CSS-first configuration (see `src/app.css`).
	// Keep this file minimal to avoid legacy plugin incompatibilities.
	theme: { extend: {} }
} satisfies Config;
